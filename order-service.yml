server:
  port: 8082

spring:
  application:
    name: order-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/the96farm-orders
  cloud:
    config:
      enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
